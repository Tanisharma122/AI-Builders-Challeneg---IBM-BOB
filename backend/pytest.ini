[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (require external dependencies)
filterwarnings =
    ignore:HF_API_KEY is not set:UserWarning
